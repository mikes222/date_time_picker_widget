name: date_time_picker_widget
description: This package brings us a way to pick date and time with fresh new UI design
version: 2.0.0
homepage: https://github.com/mikes222/date_time_picker_widget

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=1.17.0 <2.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.17.0
  stacked: ^3.0.0
  jiffy: ^5.0.0
  scrollable_positioned_list: ^0.3.5
  rxdart: ^0.27.5
  golden_toolkit: ^0.13.0


dev_dependencies:
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

